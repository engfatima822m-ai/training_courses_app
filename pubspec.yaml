name: training_courses_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1

  excel: ^2.1.0
  path_provider: ^2.1.2

  cupertino_icons: ^1.0.8

  pdf: ^3.11.3
  printing: ^5.14.3
  arabic_reshaper: 0.0.1
  bidi: 2.0.10

  # file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/employees.xlsx
    - assets/courses.xlsx
    - assets/images/logo.png
    - assets/fonts/Tajawal-Regular.ttf
    - assets/fonts/Tajawal-Bold.ttf